/* eslint-disable prettier/prettier */
import {StyleSheet} from 'react-native';
const colorExp = '#100e0f';
const styles = StyleSheet.create({
  cardTitle: {
    fontSize: 20,
    textAlign: 'center',
    color: colorExp,
    fontWeight: '500',
  },
  card: {
    width: '72%',
    borderColor: '#939393',
    borderRadius: 5,
    borderWidth: 2,
    alignContent: 'center',
    marginBottom: 20,
    padding: 10,
    backgroundColor: '#fff',
  },
});

export default styles;
